/**
 * @param name aaaaa
 */
export const Greeter = (name: string) => `This is a message: ${name}`;


export namespace OPDSettingsLib {
    export function greet(name: string): string {
        return `This is a message: ${name}`;
    }
}